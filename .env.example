# notebooklm-py-diet-mcp — runtime env
# Auth is the upstream notebooklm-py BROWSER SESSION, not env vars — there are no secrets to set here.
# NEVER hardcode cookies or tokens (AGENTS.md NEVER block). This file documents that no env config is required
# for lint/test; the MCP client supplies runtime context.
# (Intentionally empty — the cold-agent check looks for the artifact + the no-secrets statement, not values.)
